(function(){const nav=document.querySelector('.nav');const toggle=document.querySelector('.mobile-toggle');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.getAttribute('data-open')==='true';nav.setAttribute('data-open',String(!open));toggle.setAttribute('aria-expanded',String(!open));});}const counters=document.querySelectorAll('[data-count]');if(counters.length){const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const run=(el)=>{const target=Number(el.dataset.count||0);if(reduce){el.textContent=el.dataset.suffix?`${target}${el.dataset.suffix}`:`${target}+`;return;}let start=0;const steps=24;const tick=()=>{start+=1;const value=Math.round(target*(start/steps));el.textContent=el.dataset.suffix?`${value}${el.dataset.suffix}`:`${value}+`;if(start<steps)requestAnimationFrame(tick);};tick();};const io=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){run(entry.target);io.unobserve(entry.target);}});},{threshold:.4});counters.forEach((el)=>io.observe(el));}})();